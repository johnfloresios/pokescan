# Recommended: your server-side proxy, which attaches the PokéWallet API key.
EXPO_PUBLIC_POKEWALLET_PROXY_URL=https://your-api.example.com/pokewallet
# Development only. A value here is bundled into the app and is not secret.
EXPO_PUBLIC_POKEWALLET_API_KEY=
